<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Елизавета | Клинический психолог</title>
    <style>
        /* Основные цвета */
        :root {
            --pink: #ffb6d9;
            --pink-dark: #ff8ac4;
            --blue: #a6d2ff;
            --blue-dark: #7bc6ff;
            --purple: #d4b5ff;
            --purple-dark: #c79cff;
            --text: #2c2c2c;
            --text-light: #5a5a5a;
            --white-layer: rgba(255, 255, 255, 0.88);
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Georgia', 'Merriweather', serif;
            line-height: 1.8;
            color: var(--text);
            min-height: 100vh;
            position: relative;
            overflow-x: hidden;
        }
        
        /* КРАСИВЫЙ АНИМИРОВАННЫЙ ФОН */
        .background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: linear-gradient(135deg, 
                #ffcce6 0%,    /* Яркий розовый */
                #a6d2ff 50%,   /* Голубой */
                #d4b5ff 100%   /* Лавандовый */
            );
            overflow: hidden;
        }
        
        /* Волны */
        .wave {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 200%;
            height: 300px;
            background: linear-gradient(90deg, 
                rgba(255, 182, 217, 0.3) 0%,
                rgba(214, 181, 255, 0.3) 50%,
                rgba(166, 210, 255, 0.3) 100%
            );
            border-radius: 50% 50% 0 0;
            animation: wave 20s linear infinite;
        }
        
        .wave-1 {
            height: 250px;
            bottom: 0;
            opacity: 0.4;
            animation: wave 25s linear infinite reverse;
        }
        
        .wave-2 {
            height: 200px;
            bottom: 50px;
            opacity: 0.3;
            animation: wave 30s linear infinite;
        }
        
        @keyframes wave {
            0% { transform: translateX(0) rotate(0deg); }
            100% { transform: translateX(-50%) rotate(1deg); }
        }
        
        /* Плавающие частицы */
        .particle {
            position: absolute;
            border-radius: 50%;
            opacity: 0.1;
            animation: float 15s infinite ease-in-out;
            filter: blur(20px);
        }
        
        .particle-1 {
            width: 400px;
            height: 400px;
            background: var(--pink);
            top: 10%;
            left: 5%;
            animation-delay: 0s;
        }
        
        .particle-2 {
            width: 300px;
            height: 300px;
            background: var(--blue);
            bottom: 15%;
            right: 10%;
            animation-delay: -5s;
        }
        
        .particle-3 {
            width: 250px;
            height: 250px;
            background: var(--purple);
            top: 60%;
            left: 20%;
            animation-delay: -10s;
        }
        
        @keyframes float {
            0%, 100% { 
                transform: translate(0, 0) scale(1) rotate(0deg); 
            }
            33% { 
                transform: translate(30px, -40px) scale(1.1) rotate(120deg); 
            }
            66% { 
                transform: translate(-20px, 20px) scale(0.95) rotate(240deg); 
            }
        }
        
        /* Полупрозрачный белый слой */
        .page-wrapper {
            position: relative;
            background-color: var(--white-layer);
            min-height: 100vh;
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            box-shadow: inset 0 0 100px rgba(255, 255, 255, 0.3);
        }
        
        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 40px;
            position: relative;
        }
        
        /* НАВИГАЦИЯ С КРАСИВЫМИ КНОПКАМИ */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 30px 0 50px;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }
        
        .logo {
            font-size: 2.5rem;
            font-weight: 300;
            text-decoration: none;
            color: var(--text);
            letter-spacing: 1px;
            position: relative;
            padding-bottom: 5px;
        }
        
        .logo::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: linear-gradient(90deg, var(--pink), var(--blue));
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }
        
        .logo:hover::after {
            transform: scaleX(1);
        }
        
        .nav-links {
            display: flex;
            gap: 30px;
        }
        
        /* СТИЛЬНЫЕ КНОПКИ НАВИГАЦИИ */
        .nav-btn {
            text-decoration: none;
            color: var(--text);
            font-size: 1.1rem;
            padding: 12px 25px;
            position: relative;
            overflow: hidden;
            border-radius: 30px;
            background: transparent;
            transition: var(--transition);
            border: 2px solid transparent;
        }
        
        .nav-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, 
                rgba(255, 182, 217, 0.1), 
                rgba(166, 210, 255, 0.1)
            );
            transform: translateY(100%);
            transition: transform 0.3s ease;
            z-index: -1;
        }
        
        .nav-btn:hover {
            border-color: rgba(0, 0, 0, 0.1);
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }
        
        .nav-btn:hover::before {
            transform: translateY(0);
        }
        
        .nav-btn.active {
            background: linear-gradient(135deg, var(--pink), var(--blue));
            color: white;
            border-color: transparent;
        }
        
        /* КНОПКА МЕНЮ ДЛЯ МОБИЛЬНЫХ */
        .menu-toggle {
            display: none;
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, var(--pink), var(--blue));
            border: none;
            border-radius: 50%;
            cursor: pointer;
            position: relative;
            transition: var(--transition);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        
        .menu-toggle:hover {
            transform: rotate(90deg);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }
        
        .menu-toggle::before,
        .menu-toggle::after {
            content: '';
            position: absolute;
            width: 20px;
            height: 2px;
            background: white;
            left: 10px;
            transition: var(--transition);
        }
        
        .menu-toggle::before {
            top: 14px;
        }
        
        .menu-toggle::after {
            top: 24px;
        }
        
        /* Главный заголовок */
        .hero {
            text-align: center;
            padding: 80px 0 120px;
        }
        
        .hero h1 {
            font-size: 4rem;
            margin-bottom: 25px;
            font-weight: 300;
            letter-spacing: 2px;
            background: linear-gradient(135deg, var(--text), var(--text-light));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .hero p {
            font-size: 1.5rem;
            max-width: 800px;
            margin: 0 auto;
            color: var(--text-light);
            line-height: 1.7;
        }
        
        /* СЕКЦИЯ О СЕБЕ */
        .about-section {
            padding: 60px 0 100px;
        }
        
        .about-content {
            display: grid;
            grid-template-columns: 350px 1fr;
            gap: 60px;
            align-items: start;
        }
        
        .photo-container {
            position: relative;
        }
        
        .photo-frame {
            width: 100%;
            height: 450px;
            position: relative;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
            transition: var(--transition);
        }
        
        .photo-frame:hover {
            transform: translateY(-10px);
            box-shadow: 0 35px 70px rgba(0, 0, 0, 0.2);
        }
        
        .photo-frame::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, 
                rgba(255, 182, 217, 0.2), 
                rgba(166, 210, 255, 0.2)
            );
            z-index: 1;
        }
        
        .about-photo {
            width: 100%;
            height: 100%;
            object-fit: cover;
            position: relative;
            z-index: 0;
        }
        
        .about-text {
            padding-top: 20px;
        }
        
        .about-text p {
            margin-bottom: 30px;
            font-size: 1.2rem;
            line-height: 1.9;
            color: var(--text);
        }
        
        /* ЦИТАТА */
        .quote {
            background: linear-gradient(135deg, 
                rgba(255, 182, 217, 0.05), 
                rgba(166, 210, 255, 0.05)
            );
            padding: 50px;
            border-radius: 15px;
            margin: 60px 0;
            border-left: 4px solid var(--pink);
            position: relative;
            overflow: hidden;
        }
        
        .quote::before {
            content: '"';
            position: absolute;
            top: 20px;
            left: 30px;
            font-size: 120px;
            color: rgba(255, 182, 217, 0.1);
            font-family: serif;
            line-height: 1;
        }
        
        .quote p {
            margin-bottom: 25px;
            font-size: 1.25rem;
            font-style: italic;
            position: relative;
            z-index: 1;
        }
        
        /* КРАСИВАЯ КНОПКА СЕРТИФИКАТОВ */
        .certificates-btn {
            display: inline-flex;
            align-items: center;
            gap: 15px;
            background: linear-gradient(135deg, var(--pink), var(--blue));
            color: white;
            font-size: 1.2rem;
            padding: 18px 35px;
            border-radius: 50px;
            border: none;
            cursor: pointer;
            transition: var(--transition);
            box-shadow: 0 10px 30px rgba(255, 182, 217, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .certificates-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, 
                transparent, 
                rgba(255, 255, 255, 0.2), 
                transparent
            );
            transition: left 0.5s ease;
        }
        
        .certificates-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(255, 182, 217, 0.4);
        }
        
        .certificates-btn:hover::before {
            left: 100%;
        }
        
        .certificates-btn .arrow {
            transition: transform 0.3s ease;
        }
        
        .certificates-btn.active .arrow {
            transform: rotate(180deg);
        }
        
        .certificates-list {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.5s ease;
            padding-left: 30px;
            margin-top: 0;
        }
        
        .certificates-list.active {
            max-height: 500px;
            margin-top: 30px;
        }
        
        /* КОНТАКТЫ */
        .contacts-section {
            padding: 100px 0 80px;
            border-top: 1px solid rgba(0, 0, 0, 0.08);
            text-align: center;
        }
        
        .contacts-section h2 {
            font-size: 2.8rem;
            margin-bottom: 60px;
            font-weight: 300;
        }
        
        .contacts-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 60px;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }
        
        .contact-card {
            background: white;
            padding: 40px 30px;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.08);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }
        
        .contact-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, var(--pink), var(--blue));
        }
        
        .contact-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.12);
        }
        
        .contact-icon {
            font-size: 3rem;
            margin-bottom: 25px;
            background: linear-gradient(135deg, var(--pink), var(--blue));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .contact-text {
            font-size: 1.3rem;
            margin-bottom: 20px;
        }
        
        .contact-link {
            color: var(--text);
            text-decoration: none;
            transition: var(--transition);
            position: relative;
            padding-bottom: 3px;
        }
        
        .contact-link::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 1px;
            background: linear-gradient(90deg, var(--pink), var(--blue));
            transition: width 0.3s ease;
        }
        
        .contact-link:hover::after {
            width: 100%;
        }
        
        /* СУПЕР КНОПКА АНОНИМНОЙ ПОЧТЫ */
        .anonymous-btn {
            background: linear-gradient(135deg, var(--pink), var(--blue));
            color: white;
            padding: 22px 50px;
            font-size: 1.3rem;
            border-radius: 50px;
            border: none;
            cursor: pointer;
            transition: var(--transition);
            box-shadow: 0 15px 35px rgba(255, 182, 217, 0.3);
            position: relative;
            overflow: hidden;
            margin: 40px auto 0;
            display: flex;
            align-items: center;
            gap: 15px;
            justify-content: center;
        }
        
        .anonymous-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, 
                transparent, 
                rgba(255, 255, 255, 0.3), 
                transparent
            );
            transition: left 0.7s ease;
        }
        
        .anonymous-btn:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 20px 45px rgba(255, 182, 217, 0.4);
        }
        
        .anonymous-btn:hover::before {
            left: 100%;
        }
        
        .anonymous-btn::after {
            content: '';
            position: absolute;
            inset: -2px;
            background: linear-gradient(135deg, 
                var(--pink), 
                var(--blue), 
                var(--pink)
            );
            border-radius: 52px;
            z-index: -1;
            opacity: 0;
            transition: opacity 0.3s ease;
        }
        
        .anonymous-btn:hover::after {
            opacity: 1;
            animation: rotate 2s linear infinite;
        }
        
        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        /* МОДАЛЬНОЕ ОКНО */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            justify-content: center;
            align-items: center;
            z-index: 2000;
            backdrop-filter: blur(10px);
        }
        
        .modal-content {
            background: white;
            padding: 60px;
            border-radius: 25px;
            max-width: 500px;
            width: 90%;
            text-align: center;
            box-shadow: 0 40px 80px rgba(0, 0, 0, 0.25);
            position: relative;
            overflow: hidden;
        }
        
        .modal-content::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, var(--pink), var(--blue));
        }
        
        .modal h3 {
            font-size: 2rem;
            margin-bottom: 25px;
            font-weight: 300;
        }
        
        .modal-email {
            font-size: 1.5rem;
            margin: 35px 0;
            padding: 25px;
            background: linear-gradient(135deg, 
                rgba(255, 182, 217, 0.1), 
                rgba(166, 210, 255, 0.1)
            );
            border-radius: 15px;
            word-break: break-all;
            cursor: pointer;
            transition: var(--transition);
            border: 2px solid transparent;
        }
        
        .modal-email:hover {
            border-color: var(--pink);
            transform: scale(1.02);
        }
        
        .modal-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 40px;
        }
        
        .modal-btn {
            padding: 16px 35px;
            font-size: 1.1rem;
            border-radius: 50px;
            cursor: pointer;
            transition: var(--transition);
            border: 2px solid transparent;
            min-width: 160px;
        }
        
        .modal-btn.primary {
            background: linear-gradient(135deg, var(--pink), var(--blue));
            color: white;
            box-shadow: 0 10px 25px rgba(255, 182, 217, 0.3);
        }
        
        .modal-btn.primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(255, 182, 217, 0.4);
        }
        
        .modal-btn.secondary {
            background: transparent;
            border-color: var(--text-light);
            color: var(--text);
        }
        
        .modal-btn.secondary:hover {
            background: rgba(0, 0, 0, 0.05);
        }
        
        /* АДАПТИВНОСТЬ */
        @media (max-width: 1024px) {
            .container {
                padding: 30px;
            }
            
            .about-content {
                grid-template-columns: 300px 1fr;
                gap: 40px;
            }
            
            .photo-frame {
                height: 400px;
            }
        }
        
        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }
            
            nav {
                padding: 20px 0 40px;
            }
            
            .nav-links {
                display: none;
                position: absolute;
                top: 100px;
                left: 20px;
                right: 20px;
                background: var(--white-layer);
                flex-direction: column;
                padding: 30px;
                border-radius: 20px;
                box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
                backdrop-filter: blur(20px);
                z-index: 1000;
            }
            
            .nav-links.active {
                display: flex;
            }
            
            .menu-toggle {
                display: block;
            }
            
            .hero {
                padding: 60px 0 80px;
            }
            
            .hero h1 {
                font-size: 3rem;
            }
            
            .about-content {
                grid-template-columns: 1fr;
                gap: 50px;
            }
            
            .photo-frame {
                max-width: 400px;
                margin: 0 auto;
            }
            
            .contacts-grid {
                grid-template-columns: 1fr;
            }
            
            .modal-content {
                padding: 40px 25px;
            }
        }
        
        @media (max-width: 480px) {
            .hero h1 {
                font-size: 2.5rem;
            }
            
            .hero p {
                font-size: 1.2rem;
            }
            
            .quote {
                padding: 30px 25px;
            }
            
            .anonymous-btn {
                padding: 18px 30px;
                font-size: 1.1rem;
                width: 100%;
            }
            
            .modal-buttons {
                flex-direction: column;
                gap: 15px;
            }
            
            .modal-btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <!-- КРАСИВЫЙ АНИМИРОВАННЫЙ ФОН -->
    <div class="background">
        <div class="wave wave-1"></div>
        <div class="wave wave-2"></div>
        <div class="particle particle-1"></div>
        <div class="particle particle-2"></div>
        <div class="particle particle-3"></div>
    </div>
    
    <!-- Полупрозрачный белый слой -->
    <div class="page-wrapper">
        <div class="container">
            <!-- НАВИГАЦИЯ С КРАСИВЫМИ КНОПКАМИ -->
            <nav>
                <a href="#" class="logo">Елизавета</a>
                <button class="menu-toggle" id="menuToggle"></button>
                <div class="nav-links" id="navLinks">
                    <a href="#about" class="nav-btn active">Обо мне</a>
                    <a href="#contacts" class="nav-btn">Контакты</a>
                </div>
            </nav>

            <!-- ГЛАВНЫЙ ЗАГОЛОВОК -->
            <header class="hero">
                <h1>Елизавета</h1>
                <p>Клинический психолог, арт-терапевт. 20 лет психологической практики, 17 лет преподавания психологии в высшей школе</p>
            </header>

            <!-- ОСНОВНОЙ КОНТЕНТ -->
            <section class="about-section" id="about">
                <div class="about-content">
                    <div class="photo-container">
                        <div class="photo-frame">
                            <img src="my-photo.jpg" alt="Психолог Елизавета" class="about-photo">
                        </div>
                    </div>
                    
                    <div class="about-text">
                        <p>Здравствуйте! Я — клинический психолог, арт-терапевт, системный семейный терапевт, специалист в области психологического тренинга, супервизор.</p>
                        
                        <p>За плечами 20 лет психологической практики, 17 лет преподавания психологии в высшей школе и более 30 авторских тренингов и семинаров.</p>
                        
                        <p>Мои профессиональные ценности в работе: бережное отношение к клиенту, результативность, качество.</p>
                        
                        <div class="quote">
                            <p>За долгие годы практики профессия врастает в нас. Быть психологом давно стало частью меня. Важной частью.</p>
                            
                            <p>Чувствовать боль другого, поддерживать, помогать преодолевать преграды внешние и внутренние, помогать расти, развиваться, достигать, сопереживать, быть, грустить и радоваться с вами, встречать нового клиента. Идти рядом с ним в его боли.</p>
                            
                            <p>А потом с улыбкой смотреть ему в след, когда дальше он — сам, сильнее и увереннее, надежнее и с опорой на себя. Смотреть и радоваться, что я ему больше не нужна. И снова встречать нового клиента.</p>
                            
                            <p>Это моя работа. Это большая и важная часть моей жизни.</p>
                        </div>
                        
                        <p>Если Вам нужна помощь, жду Вас на моих консультациях.</p>
                        
                        <p>С уважением, ваш психолог Елизавета.</p>
                        
                        <button class="certificates-btn" id="certificatesBtn">
                            <span>Мои дипломы и сертификаты</span>
                            <span class="arrow">▼</span>
                        </button>
                        
                        <div class="certificates-list" id="certificatesList">
                            <p>Диплом о высшем психологическом образовании</p>
                            <p>Сертификат арт-терапевта</p>
                            <p>Сертификат по когнитивно-поведенческой терапии</p>
                            <p>Сертификат системного семейного терапевта</p>
                            <p>Удостоверение о повышении квалификации</p>
                            <p>Сертификат супервизора</p>
                        </div>
                    </div>
                </div>
            </section>

            <!-- КОНТАКТЫ -->
            <section class="contacts-section" id="contacts">
                <h2>Контакты</h2>
                
                <div class="contacts-grid">
                    <div class="contact-card">
                        <div class="contact-icon">📞</div>
                        <div class="contact-text">
                            Телефон:<br>
                            <a href="tel:+79991234567" class="contact-link">+7 (999) 123-45-67</a>
                        </div>
                    </div>
                    
                    <div class="contact-card">
                        <div class="contact-icon">✉️</div>
                        <div class="contact-text">
                            Email:<br>
                            <a href="mailto:elizabeth@example.com" class="contact-link">elizabeth@example.com</a>
                        </div>
                    </div>
                </div>
                
                <button class="anonymous-btn" id="anonymousBtn">
                    <span>✉️</span>
                    <span>Написать анонимно</span>
                </button>
            </section>
            
            <!-- ПОДВАЛ -->
            <footer style="margin-top: 100px; padding-top: 50px; border-top: 1px solid rgba(0,0,0,0.08); text-align: center; color: #666; font-size: 1rem;">
                <p>© 2024 Елизавета. Клинический психолог.</p>
            </footer>
        </div>
    </div>

    <!-- МОДАЛЬНОЕ ОКНО -->
    <div class="modal" id="emailModal">
        <div class="modal-content">
            <h3>Написать анонимно</h3>
            <p>Вы можете написать мне на эту почту, не указывая своё имя:</p>
            
            <div class="modal-email" id="emailToCopy">
                blisslovegood@outlook.com
            </div>
            
            <p style="margin-bottom: 20px; font-size: 0.95rem; color: #666;">
                Письмо будет абсолютно конфиденциально
            </p>
            
            <div class="modal-buttons">
                <button class="modal-btn primary" id="copyBtn">
                    Скопировать почту
                </button>
                <button class="modal-btn secondary" id="closeModal">
                    Закрыть
                </button>
            </div>
        </div>
    </div>

    <script>
        // Мобильное меню
        const menuToggle = document.getElementById('menuToggle');
        const navLinks = document.getElementById('navLinks');
        
        menuToggle.addEventListener('click', () => {
            navLinks.classList.toggle('active');
            menuToggle.classList.toggle('active');
        });
        
        // Активная кнопка навигации
        document.querySelectorAll('.nav-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
                this.classList.add('active');
                navLinks.classList.remove('active');
                menuToggle.classList.remove('active');
            });
        });
        
        // Сертификаты
        const certificatesBtn = document.getElementById('certificatesBtn');
        const certificatesList = document.getElementById('certificatesList');
        
        certificatesBtn.addEventListener('click', () => {
            certificatesList.classList.toggle('active');
            certificatesBtn.classList.toggle('active');
        });
        
        // Анонимная почта
        const anonymousBtn = document.getElementById('anonymousBtn');
        const emailModal = document.getElementById('emailModal');
        const closeModal = document.getElementById('closeModal');
        const copyBtn = document.getElementById('copyBtn');
        const emailToCopy = document.getElementById('emailToCopy');
        
        anonymousBtn.addEventListener('click', () => {
            emailModal.style.display = 'flex';
        });
        
        closeModal.addEventListener('click', () => {
            emailModal.style.display = 'none';
        });
        
        emailModal.addEventListener('click', (e) => {
            if (e.target === emailModal) {
                emailModal.style.display = 'none';
            }
        });
        
        copyBtn.addEventListener('click', () => {
            navigator.clipboard.writeText(emailToCopy.textContent.trim())
                .then(() => {
                    copyBtn.textContent = 'Скопировано!';
                    copyBtn.style.background = 'linear-gradient(135deg, #4CAF50, #45a049)';
                    
                    setTimeout(() => {
                        copyBtn.textContent = 'Скопировать почту';
                        copyBtn.style.background = 'linear-gradient(135deg, var(--pink), var(--blue))';
                    }, 2000);
                })
                .catch(err => {
                    console.error('Ошибка при копировании: ', err);
                    copyBtn.textContent = 'Ошибка';
                });
        });
        
        // Плавная прокрутка
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                
                const targetId = this.getAttribute('href');
                if (targetId === '#') return;
                
                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    window.scrollTo({
                        top: targetElement.offsetTop - 100,
                        behavior: 'smooth'
                    });
                }
            });
        });
        
        // Анимация при загрузке
        window.addEventListener('load', () => {
            document.body.style.opacity = '0';
            document.body.style.transition = 'opacity 0.5s ease';
            
            setTimeout(() => {
                document.body.style.opacity = '1';
            }, 100);
        });
    </script>
</body>
</html>
